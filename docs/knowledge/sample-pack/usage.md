# Sample pack usage — Terminal demo & Research observation

## Terminal demo

Use this pack as the **knowledge script** when walking a revenue-chain story in Terminal:

1. Refresh **Sample knowledge pack (G293)** on Operator / Admin (reads `/v1/adapters` meta) or open `/v1/demo/sample-pack/INDEX.md` on the demo gateway.
2. Confirm `GET /v1/knowledge/status` nests `sample_knowledge_pack_product` with `crud=false`.
3. Start at [INDEX.md](INDEX.md); open linked extract pages — do not invent UI CRUD.
4. Narrate the [assembled_chain.md](assembled_chain.md) map; pause at honesty holds (dual AR, Contract absence).
5. If Terminal surfaces are Foundation/OpenAPI only, say so: this pack does **not** authorize CRM/Sales/Finance/Delivery product routes.
6. Record demo gaps with [TERMINAL_DEMO_GAP.md](../../research/templates/TERMINAL_DEMO_GAP.md) — mark Unknown where live side effects are unproven.

**Data mode:** knowledge / synthetic narrative only unless a live T2–T3 artifact exists (intake still **0 Complete** at PHX-G163).

## Research observation

| Use | How |
|-----|-----|
| Evidence planning | Map needed live artifacts to chain stages without assuming conversion semantics |
| AR Board / Promote prep | Cite extract paths + this assembly; still require Phoenix ADR for Promote |
| Gap templates | Fill Sample/Order knowledge gap tables from extracts, not from invented modules |

Research must not treat this pack as field evidence. Live T2/T3 remains gated.

## Explicit non-use

- Not a Marketplace knowledge pack contract (PHX-M16 / K10).
- Not Brain insight execute input and not Twin authorize material.
- Not a substitute for product Gate + Package Surface slices on rewrite-boundary ADRs (0312–0318).
