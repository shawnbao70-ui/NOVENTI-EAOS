# Coding Authorization Summary — Brain Confidence/Bias Honesty (G389)

## Milestone

**PHX-G389** — Brain `/status` honesty for confidence/bias posture.

## Alembic

**none** — tip remains `0092_finance_realized_fx_gl_bridge_g372`.

## Authorized

1. Deepen `GET /v1/brain/status` with closed fields:
   `confidence_field_required=true`, `bias_notes_surface=insight_payload`,
   `confidence_drives_execution=false`, `commercial_auto_write=false`.
2. OpenAPI + runtime DTO parity; confidence never implies commercial write.

## Out

Handoff #2 (G390), advisory expand (G391), host installs, Marketplace PSP.

## Product Owner response

**Approve — Batch-B; auto-continue G390–G393.**
