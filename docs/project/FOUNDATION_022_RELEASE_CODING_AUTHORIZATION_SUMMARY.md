# Coding Authorization Summary — Foundation 0.2.2 Release Cut (G376)

## Milestone

**PHX-G376** — package version cut to `0.2.2` at tip `0092`.

## Alembic

**none** — tip remains `0092_finance_realized_fx_gl_bridge_g372`.

## Authorized

1. Bump `pyproject.toml` version `0.2.1` → `0.2.2`.
2. Align RELEASE_MANIFEST.yaml version + alembic_head `0092`; ops/checklist/
   compatibility/CHANGELOG release note for 0.2.2 train.
3. Update MASTER_PLAN / ROADMAP baseline pointers that claim current package
   version (active only; no historical rewrite spam).
4. No business CRUD.

## Out

Knowledge invent (G377), Industry runtime, Marketplace PSP, host installs.

## Product Owner response

**Approve — Platform thin open batch.** Auto-continue G377.
