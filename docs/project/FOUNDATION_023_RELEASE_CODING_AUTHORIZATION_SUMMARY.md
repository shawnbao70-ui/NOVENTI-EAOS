# Coding Authorization Summary — Foundation 0.2.3 Release Cut (G404)

## Milestone

**PHX-G404** — package version cut to `0.2.3` at tip `0092`.

## Alembic

**none** — tip remains `0092_finance_realized_fx_gl_bridge_g372`.

## Authorized

1. Bump `pyproject.toml` / `eaos_sdk.__version__` `0.2.2` → `0.2.3`.
2. Align RELEASE_MANIFEST.yaml version + alembic_head `0092`; ops/checklist/
   compatibility/CHANGELOG release note for 0.2.3 train.
3. Update MASTER_PLAN / ROADMAP baseline pointers that claim current package
   version (active only; no historical rewrite spam).
4. No business CRUD; no external PSP invent; no bank-file import.

## Out

V2.0 readiness checklist (G405); host installs; ENABLE_*_NETWORK invent ON.

## Product Owner response

**Approve — Batch-D; auto-continue G405.**
