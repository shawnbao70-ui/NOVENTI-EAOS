# Coding Authorization Summary — Remediation P0-2 Contract Shards (G408)

## Milestone

**PHX-G408** — shard contracts；required PR set ≤10 minutes.

## Alembic

**none** — tip remains `0092_finance_realized_fx_gl_bridge_g372`.

## Authorized

1. `tests/contracts/shards.yaml` domain shards + `pr_required` path list.
2. Runner `scripts/run_contract_shard.py` printing `DURATION_SECONDS`.
3. `docs/release/CONTRACT_SHARDS.md` ownership + budget honesty.
4. Contract proving shard inventory + budget definition.
5. No flaky skips；no feature work；no package bump.

## Out

Helm parity（G409）；CI workflows（G410）；governance boards（G411）；Docker host install.

## Product Owner response

**Approve — Milestone PHX-G408（2026-07-27）。STOP after TRACK-G408。**
