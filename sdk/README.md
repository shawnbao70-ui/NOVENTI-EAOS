# sdk/

Client and extension SDKs for EAOS.

## Package

`eaos_sdk` — Phoenix Foundation SDK surface (PHX-R17).

```python
from eaos_sdk import build_tenant_context, unwrap, list_openapi_contracts
```

## Status

PHX-R17 Foundation release surface. No privileged bypasses. No FastAPI client stack required.
